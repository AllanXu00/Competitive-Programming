#include <iostream>
#include <math.h> 
using namespace std;

int main()
{
    int a, x; 
    cin >> a;
    x = sqrt (a);
    cout  << "The largest square has side length " << x << "." ;
      
   return 0;
}
